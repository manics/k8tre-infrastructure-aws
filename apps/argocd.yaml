global:
  logging:
    level: debug
  
configs:
  params:
    server.insecure: true
  # https://github.com/argoproj/argo-helm/issues/1817
  cm:
    kustomize.buildOptions: "--enable-helm --load-restrictor LoadRestrictionsNone"
  # Clusters are added as secrets
